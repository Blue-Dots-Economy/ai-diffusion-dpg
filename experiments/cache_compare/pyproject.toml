[project]
name = "cache-compare"
version = "0.1.0"
description = "Standalone cache-strategy comparison harness for #311."
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40",
    "openai>=1.50",
    "pyyaml>=6.0",
]
